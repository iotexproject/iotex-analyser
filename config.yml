server:
#  http: 127.0.0.1:8989
  httpAdminPort: 8988
  plugins:
    - block.so
    - blockaction.so
    - blockmeta.so
    - blockmeta_reward.so
    - delegates.so
    - iotexprice_worker.so
database:
  dsn: root:root@tcp(127.0.0.1:3306)/analyser
iotex:
  #catchUpMode: true
  chainEndPoint: api.testnet.iotex.one:80
blockDB:
  dbPath: /media/millken/SAMSUNG/iotex-var/data/chain.db
log:
  zap:
    development: false
    level: debug
    encoding: console
    disableCaller: false
    disableStacktrace: false
    outputPaths: ["stderr"]
    errorOutputPaths: ["stderr"]
  stdLogRedirect: false 
  stderrRedirectFile: std.log