server:
#  http: 127.0.0.1:8989
  plugins:
   - block.so
   - blockmeta.so
database:
  dsn: root:root@tcp(127.0.0.1:3306)/iotex-analyser
iotex:
  chainEndPoint: api.testnet.iotex.one:80
  batchSize: 512
blockDB:
  dbPath: /media/millken/SAMSUNG/iotex-var/data/chain.db
log:
  zap:
    development: true
    level: debug
    encoding: console
    disableCaller: false
    disableStacktrace: false
    outputPaths: ["stderr"]
    errorOutputPaths: ["stderr"]
  stdLogRedirect: true  